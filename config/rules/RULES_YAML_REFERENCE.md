# RackBrain rule YAML reference (shortcut)

Several rule files in this folder include comments pointing here.

Canonical docs:

- Rule authoring guide: `docs/RULE_AUTHORING.md`
- Rule schema summary: `config/RULES_REFERENCE.md`
- Config keys and path semantics: `config/CONFIG_REFERENCE.md`

Quick links for rule authors:

- Common template placeholders live in `rackbrain/services/comment_renderer.py`.
- Matching logic lives in `rackbrain/core/classification.py`.
- Rule parsing lives in `rackbrain/core/rules_engine.py`.

